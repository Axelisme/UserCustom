# config-single-source-validation — User exercises the converged config path

| Ticket field | Value |
|---|---|
| id | config-single-source-validation |
| status | open |
| depends_on | config-single-source |

<!-- status is exactly open | active | closed. When status becomes closed, add a `disposition` row
     directly below it — resolved | superseded | out-of-scope | hard-stop; omit the row while status
     is open or active. -->

**Resolve by:** After `config-single-source` is lane-ready, the user runs the application and
confirms the merged configuration value is correct; user presence is required.

## Outcome
Not finished.

## Current

Blocked on `config-single-source`; nothing to record until the user exercises the result.
