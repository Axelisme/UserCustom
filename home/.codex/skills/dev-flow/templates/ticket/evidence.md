# {{TICKET_ID}} — Acceptance appendix

<!-- Workflow-scoped Acceptance appendix for named difficult claims.
     Orchestrator precreates one exact target per workflow by copying this
     template to a fresh collision-free path under tickets/<ticket-id>/ and
     places that exact path plus covered claim IDs in both worker and review
     briefs. No assignment grants no task-record evidence mutation.
     One Dev-flow template owns the Subject/Evidence/Residuals spine. Do not
     duplicate this format elsewhere. The appendix binds the fixed candidate
     without judging Acceptance; the Orchestrator owns wording, state, and
     Resolution. -->

## Subject
<!-- Fixed candidate: commit SHA and tree, managed lane, covered Acceptance
     claim IDs, and deterministic command surface. A moving subject makes
     conclusions unverifiable. Updated sequentially for automatic corrections
     within the same workflow; a later separately dispatched workflow receives
     a fresh target and leaves earlier evidence byte-for-byte unchanged. -->

## Evidence
<!-- Method, observations, and artifact pointers for the covered claims.
     Describe the validation method actually run, what was observed, and
     pointers to owned raw artifacts when needed. Do not judge Acceptance,
     create or rewrite claims, or mark satisfaction. Raw logs remain with
     their runtime owner. The acceptor will directly check observable claims
     and judge only whether this method reasonably supports the difficult
     claim. -->

## Residuals
<!-- Limitations, unreproduced conditions, and what remains unproved.
     State what this method does not establish and any deferred conditions.
     `None.` is a valid answer — say it explicitly rather than omitting it.
     A missing, stale-subject, or method-inadequate appendix blocks
     completion or Acceptance. -->
