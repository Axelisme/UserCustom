# Orchestrate v183 migration

Orchestrate v183 carries the transaction and report corrections forward without rewriting v181 or
v182.

- Agent acceptance transactions use Git's non-dereferencing ref operations and refuse a symbolic
  `acceptance-open` interleave before accepted authority or telemetry can change.
- Same-SHA user evidence retains its independent `acceptance-user` marker while unresolved agent
  counts, active agent spans, warnings, rates, and coverage remain represented by report replay.
- The verifiable v181 and v182 migrations and manifests remain unchanged; this exact-next v183
  package adds the corrected source, migration, and manifest.

No installation, pinning, live-HOME mutation, push, or remote publication is performed by this
migration.
