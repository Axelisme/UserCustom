# Migrate to Orchestrate v134

## From

v133

## Observable changes

Every CLI JSON response identifies the installed executable version. Pin drift stays non-blocking, task mutation requires a verified installed release, and `pin set` is the sole pin writer. Explicit Pi lane turn budgets can emit one best-effort early handoff notice.

## Active-task impact

No automatic conversion, cleanup or interruption occurs. Existing branches, refs, worktrees, candidate state and receipts remain unchanged. The private turn-threshold event was tested with pi-subagents 0.37.2; another package version does not block dispatch, but the best-effort early notice may be lost while the upstream hard budget remains active.

## Manual steps

1. Read and apply each guide from the repo's pinned version plus one through v134.
2. Verify installed documents, profiles and mandatory runtime assets with `doctor`.
3. For Pi, reload the matched Adapter and exercise an isolated dispatch and attestation.
4. After manual coordination and separate authority, run `pin set --root <repo>`.

## Verification

Confirm CLI success, error and parser-error JSON carry `orchestrate_version`; run release doctor; verify pin drift does not stop task work; and retain exact Adapter run/process terminal evidence.

## Rollback

Stop before `pin set` if verification fails. Restore the prior installed release through the normal atomic setup path without deleting active task state; the prior pin remains the last adopted-release marker.
