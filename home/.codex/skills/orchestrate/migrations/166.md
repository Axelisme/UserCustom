# Migrate to Orchestrate v166

## Purpose

Compress the always-read dev-flow and orchestrate skill bodies without changing their lifecycle,
admission, authority, commands, pointers, exceptions, or completion criteria.

## Observable changes

- `orchestrate/SKILL.md` expresses the same nine-step lifecycle and exceptions with shorter rules and
  minimal rationale.
- Lane comments are described as mutable bounded annotations, matching `lane comment --text|--clear`.
- The ninth-and-later lane warning retains its `uncollected > 0` condition and remains non-blocking.
- `dev-flow/SKILL.md` preserves S0, custody, single-store, routing, and record semantics in a shorter
  form.
- `dev-flow/scripts/plan.py` corrects stale version and heading-pointer comments; behavior is unchanged.

## Active-task impact

None. Existing task refs, lanes, worktrees, trailers, acceptance state, and records remain valid.

## Manual steps

None.

## Verification

Run `orchestrate.py doctor`, the repository test suite, and compare the revised skill rules against
v165 at each lifecycle step.

## Rollback

Restore the v165 documents and manifest together. Do not pair a v165 manifest with v166 documents.
