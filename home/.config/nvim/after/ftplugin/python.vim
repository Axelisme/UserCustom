setlocal tabstop=4 " A TAB character looks like 4 spaces
setlocal expandtab " Pressing the TAB key will insert spaces instead of a TAB character
setlocal softtabstop=4 " Number of spaces inserted instead of a TAB character
setlocal shiftwidth=4 " Number of spaces inserted when indenting
