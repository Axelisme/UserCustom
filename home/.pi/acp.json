{
  "enabled": true,
  "debug": false,
  "compress": {
    "maxContextLimit": "90%",
    "emergencyThresholdPercent": "95%",
    "minCompressRange": 20000,
    "nudgeGrowthTokens": 36150,
    "minPressureBenefitTokens": 20000,
    "promptPack": "lean"
  },
  "absorb": {
    "enabled": true,
    "minToolTokens": 3000,
    "contextThresholdPct": 0,
    "batchable": true
  },
  "toolPrompts": {
    "compress": {
      "promptGuidelines": [
        "Submit every range acp_status lists as its own entry in one call: the earliest entry breaks the cache prefix, so more entries cost nothing while a second compression breaks it again. What falls between entries stays untouched — leave anything still load-bearing there."
      ]
    }
  }
}
